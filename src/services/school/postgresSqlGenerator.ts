/**
 * ============================================================================
 * LEGACY SQL GENERATOR (DISABLED / COMMENTED OUT FOR PRODUCTION ARCHITECTURE)
 * Real database schemas are provisioned via PostgreSQL init scripts directly.
 * ============================================================================
 */

export function generatePostgresInsertSQL(): string {
  return '-- Mock data generation disabled in production architecture.';
}
